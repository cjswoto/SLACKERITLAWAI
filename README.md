# Slacker IT Trainer

This desktop app fine-tunes Hugging Face models and exports them to GGUF. It uses `ttkbootstrap` for a dark themed interface.

## Setup

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python -m slacker_trainer.gui
```

## Tests

```bash
pytest
```
